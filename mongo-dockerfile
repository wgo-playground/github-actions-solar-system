# Use the official MongoDB image as the base image
FROM mongo:latest

# Set environment variables for the MongoDB user
ENV MONGO_INITDB_ROOT_USERNAME=skatravath
ENV MONGO_INITDB_ROOT_PASSWORD=skatravath

# Expose the default MongoDB port
EXPOSE 27017

# Start the MongoDB server
CMD ["mongod"]
